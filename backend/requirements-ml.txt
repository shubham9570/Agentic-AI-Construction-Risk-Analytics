# Optional heavy ML/vision deps (NOT needed for core API).
# Install only on machines that run PPE detection or local model inference.
ultralytics~=8.3.0
opencv-python~=4.10.0
scikit-learn~=1.5.0
joblib~=1.4.0
torch~=2.4.0
